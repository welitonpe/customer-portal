import { Button } from "@clayui/core";

const Footer = ({ props }) => {
  return (
    <div className="d-flex justify-content-center">
      <Button displayType="primary" className="rounded-lg">
        Get Started
      </Button>
    </div>
  );
};

export default Footer;
